// `in` is a Kotlin keyword (e.g. `for (x in list)`) — a package segment
// with that name must be backtick-escaped here, even though the actual
// directory on disk is still a plain `in/` folder.
package `in`.manojreddy.raa_podham

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity()
